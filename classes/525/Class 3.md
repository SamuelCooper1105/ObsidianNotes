data types are mostly abstractions, anything more then a byte, or word has some kind of abstraction applied to it. bytes, words, longs, and variations of words.

word size is the maximum register length size.
array is a collection of bytes, accessed by memory addressing offsets uses indtiect memory accessing mode.
find the base address using a pointer, 
# everything is byte accessable
number is the amount of bytes
or the size of the unit that you are addressing

n-dimentional arrays are coreated from a series of one dimensional array. everything is getting stored in a sequential single dimension order.
